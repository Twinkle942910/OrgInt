<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorText</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Tabs theme. -->
    <style name="TabLayoutStyle" parent="Widget.Design.TabLayout">
        <item name="android:minHeight">@dimen/tab_max_height</item>
        <item name="tabIndicatorColor">#FFFFFF</item>
        <item name="tabIndicatorHeight">2dp</item>
        <item name="tabPaddingStart">12dp</item>
        <item name="tabPaddingEnd">12dp</item>
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabTextAppearance">@style/TabTextAppearanceStyle</item>
        <item name="tabSelectedTextColor">?android:textColorPrimary</item>
    </style>

    <!-- Tabs text theme. -->
    <style name="TabTextAppearanceStyle" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">?android:textColorSecondary</item>
        <item name="android:paddingBottom">16dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="textAllCaps">true</item>
    </style>

    <!-- Tabs icons theme. -->
    <style name="TabIconStyle" >
        <item name="android:paddingBottom">10dp</item>
        <item name="android:paddingTop">12dp</item>
    </style>

    <!-- Text Input theme. -->
    <style name="StyledTilEditTextFloatingLabel" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/edit_text_floating_label_size</item>
    </style>

    <style name="StyledTilEditTextLayout">
        <item name="android:textColorHint">@color/colorGrey</item>
    </style>

    <style name="StyledTilEditTextTheme">
        <item name="android:imeOptions">actionNext</item>
        <item name="android:singleLine">true</item>
        <item name="colorControlNormal">@color/colorGrey</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorPrimary</item>
        <item name="android:textSize">@dimen/edit_text_label_size</item>
    </style>

    <style name="StyledTilEditText">
        <item name="android:textColor">@color/colorNotificationText</item>
        <item name="android:theme">@style/StyledTilEditTextTheme</item>
        <item name="android:paddingTop">4dp</item>
    </style>

    <!-- Date Picker theme. -->
    <style name="MyDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorControlNormal">@android:color/white</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="textColorAlertDialogListItem">@android:color/white</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:windowBackground">@drawable/date_dialog_background</item>
    </style>

    <!-- Dialog theme. -->
    <style name="AppCompatAlertDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/colorGrey</item>
        <item name="android:background">@color/colorWhite</item>
    </style>

    <!-- Switch theme. -->
    <style name="MySwitch" parent="Theme.AppCompat.Light">
        <!-- active thumb & track color (30% transparency) -->
        <item name="colorControlActivated">@color/colorPrimaryDark</item>

        <!-- inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/colorSwitchActivated</item>

        <!-- inactive track color (30% transparency) -->
        <item name="android:colorForeground">@color/colorGrey</item>

        <item name="android:textColor">@color/colorGrey</item>
    </style>

    <!-- CheckBox theme. -->
    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorGrey</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>

    <!-- CheckBox theme. -->
    <style name="MyCardStyle" parent="Theme.AppCompat.Light">
        <item name="cardCornerRadius">2dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardUseCompatPadding">true</item>
        <item name="cardBackgroundColor">@color/colorWhite</item>
    </style>

</resources>
